<!DOCTYPE html>
<html>
    {% include "partials/header.html" %}
    <body>
        <div class="container">
            <h1>Cuisines</h1>

            {% if cuisines is not empty %}
                <p>Cuisine options listed below:</p>
                <ul>
                    {% for cuisine in cuisines %}
                        <li>{{ cuisine.getCuisineType }}</li>
                    {% endfor %}

            {% endif %}

            {% include "partials/cuisine_form.html" %}

            <br>
            <p><a href='/'>Home</a></p>
        </div>
    </body>
</html>
